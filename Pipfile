[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastf1 = ">=2.3.0"
pydantic = ">=1.10.2"
beanie = ">=1.12.1"
python-dotenv = "*"

[dev-packages]
ipykernel = "*"

[requires]
python_version = "3.10"

[scripts]
"start" = "cd src/nikita && python nikita.py" 
"docker:start" = "docker-compose up -d --build"
"docker:stop" = "docker-compose down"
"docker:restart:nikita" = "docker-compose up -d --build --no-deps --force-recreate nikita"